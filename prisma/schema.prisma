generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Todos {
  done    Boolean?
  title   String
  todo_id Int      @default(autoincrement()) @id
}