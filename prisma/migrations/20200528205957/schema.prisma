generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model Todo {
  id      Int     @default(autoincrement()) @id
  title   String
  done    Boolean
}